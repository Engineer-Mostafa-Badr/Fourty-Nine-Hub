import 'package:flutter/material.dart';


class ChanceDitailsWidget extends StatelessWidget {
  const ChanceDitailsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(
          child: Text("Chance DEeeeeeeeeeeee"),
        )
      ],
    );
  }
}
