import 'package:flutter/material.dart';

class ReferralCodePartScreen extends StatelessWidget {
  const ReferralCodePartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
