import 'package:flutter/material.dart';

class PicturePartScreen extends StatelessWidget {
  const PicturePartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}