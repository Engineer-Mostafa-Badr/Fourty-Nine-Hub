import 'package:flutter/material.dart';

class IdPartScreen extends StatelessWidget {
  const IdPartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}