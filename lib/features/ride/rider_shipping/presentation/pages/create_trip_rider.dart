import 'package:flutter/material.dart';

class CreateTripRider extends StatelessWidget {
  const CreateTripRider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
