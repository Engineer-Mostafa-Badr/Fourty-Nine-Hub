import 'package:flutter/material.dart';

class ReelsPostContent extends StatelessWidget {
  const ReelsPostContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Reels Post Content'));
  }
}