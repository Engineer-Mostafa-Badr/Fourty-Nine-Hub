import 'package:flutter/material.dart';

class PostsSearchView extends StatelessWidget {
  const PostsSearchView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
