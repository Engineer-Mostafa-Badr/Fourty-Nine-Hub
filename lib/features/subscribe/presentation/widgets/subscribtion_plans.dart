import 'package:flutter/material.dart';

import '../../domain/entities/subscribtion_plans_entity.dart';

class SubscribtionPlansWidget extends StatelessWidget {
  final SubscribtionPlansEntity subscribePlans;
  const SubscribtionPlansWidget({
    super.key,
    required this.subscribePlans,
  });


  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
